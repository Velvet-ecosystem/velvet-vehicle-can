[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "velvet-vehicle-can"
version = "0.1.0"
description = "Velvet CAN vehicle subsystem - read-only CAN learning, fingerprinting, and safety qualification"
readme = "README.md"
license = { file = "LICENSE" }
authors = [
  { name = "Velvet AI", email = "noreply@velvet.ai" }
]
requires-python = ">=3.8"

# Core dependencies (minimal)
dependencies = []

# Optional dependency for hardware CAN access
[project.optional-dependencies]
hardware = ["python-can>=4.0.0"]

[tool.setuptools]
packages = ["velvet_vehicle_can"]
include-package-data = true

[project.urls]
Homepage = "https://github.com/Velvet-ecosystem"
Documentation = "https://github.com/Velvet-ecosystem/velvet-vehicle-can/tree/main/docs"
Repository = "https://github.com/Velvet-ecosystem/velvet-vehicle-can"
Issues = "https://github.com/Velvet-ecosystem/velvet-vehicle-can/issues"
